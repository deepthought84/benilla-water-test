# Rust build artifacts
/target/
**/*.rs.bk
# Cargo.lock IS committed (this workspace produces binaries).

# --- Blizzard-owned content: NEVER commit ---
# A legally-installed 1.12.1 client, read at runtime; not redistributed.
/WoW
# Cached / extracted assets derived from an install.
/extracted/
/out/
# Real client files used as test fixtures (document expected paths, do not commit).
**/tests/fixtures/

# Profiling output (`--features bevy/trace_chrome`).
trace-*.json

# OS / editor cruft
.DS_Store
*.swp
/.idea/

TODO
ideas.md
water-style-shots

# The client's own local state, written beside the executable at runtime: CVars, macros, chat and
# window layout, saved variables, and the per-character camera and addon settings. One machine's
# play session, not the project's.
/benilla-config/
# The run log.
/benilla.log
